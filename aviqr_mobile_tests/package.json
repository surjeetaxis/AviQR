{
  "name": "aviqr-mobile-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone logic and unit tests for AviQR mobile apps",
  "scripts": {
    "test": "jest --config jest.logic.config.js --no-coverage",
    "test:logic": "jest --config jest.logic.config.js --no-coverage",
    "test:watch": "jest --config jest.logic.config.js --watch"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "axios": "^1.7.2"
  }
}