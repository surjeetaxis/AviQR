{
  "name": "aviqr-ui-tests",
  "version": "1.0.0",
  "description": "Playwright UI automation for AviQR — all pages, all user types",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
